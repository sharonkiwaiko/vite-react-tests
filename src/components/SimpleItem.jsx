import React from "react";
//{props.post.title}
//{props.post.content}
 const Item = (props) => {
  return (
    <div>
      <h1>Title text</h1>
      <p>content</p>
    </div>
  );
};

export default Item;
